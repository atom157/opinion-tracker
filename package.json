{
  "name": "opinion-portfolio-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Opinion Portfolio Tracker (static + Vercel serverless API proxy)",
  "engines": {
    "node": ">=18"
  }
}
